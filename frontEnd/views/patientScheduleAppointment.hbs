<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>
<body>
    <nav>
        <h3>Medical Database</h3>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/auth/logout">Logout</a></li>
        </ul>
    </nav>

    <div class="container mt-4">
        <div class="card">
            <div class="card-header">
                Schedule Appointment
            </div>
            <div class="card-body">
                <form action="/auth/scheduleAppointment" method="POST">

                    <div class="form-group">
                        <label for="doctor">Choose a doctor:</label>
                        <select class="form-control" id="doctor" name="doctor" required="true">
                            <option value="ralph">John</option>
                            <option value="bob">Bob</option>
                            <option value="frank">Frank</option>
                        </select>
                    </div>

                     <div class="form-group">
                        <label for="date">Choose a date:</label>
                        <input class="form-control" type="date" id="date" name="date" required="true" min=>
                    </div>

                    <div class="form-group">
                        <label for="time">Choose a time:</label>
                        <input class="form-control" type="time" id="time" name="time" required="true" min="09:00" max="18:00">
                    </div>

                    <button type="submit" class="btn btn-primary">Schedule Appointment</button>
                </form>
                <script>
                    setDate();
                    function setDate(){
                        today = new Date();
                        var dd = today.getDate();
                        var mm = today.getMonth()+1;
                        var yyyy = today.getFullYear();
                        if(dd<10) dd='0'+dd;
                        if(mm<10) mm='0'+mm;
                        var currDate = (yyyy+'-'+mm+'-'+dd);
                        document.getElementById("date").min = currDate;


                    }
                </script>
            </div>
        </div>
        {{#if message}}
            <h4 class="aler alert-success mt-4">{{message}}</h4>
        {{/if}}
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
</body>
</html>