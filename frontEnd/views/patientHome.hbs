<!DOCTYPE html>
<html lang="en">
<script>
    function addTableData(items) {
      const table = document.getElementById("tableBody");
      items.forEach( item => {
        let row = table.insertRow();
        let appointmentID = row.insertCell(0);
        let startTime = row.insertCell(1);
        let doctorID = row.insertCell(2);
        let cancel = row.insertCell(3);
        var button = document.createElement("input");
        button.type = "button";
        button.value = "Cancel";
        button.onclick = function(){
            var row = this.parentNode.parentNode;
            row.parentNode.removeChild(this.parentNode.parentNode);
            };
            
        appointmentID.textContent = item.appointmentID;
        startTime.textContent = item.startTime;
        doctorID.textContent = item.doctorID;      
        cancel.append(button);
      });
    }

    async function getData(){
        const res = await fetch('/auth/viewActiveAppointments');
        const data = await res.json();
        addTableData(data);
    }

</script>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>
<body onload="getData()">
    <nav>
        <h3>Medical Database</h3>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/">Logout</a></li>
        </ul>
    </nav>

    <header class="jumbotron">
        <div class="container">
            <div class="row">
                <div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12">
                <h1 class="text-center">Home</h1>
                </div>
            </div>
        </div>
    </header>
    <div class="container" style="display: flex;">
        <div style="width: 50%;" class="card">
            <div class="card-body">
                <table class="table table-striped table-responsive" id="apptTable">
                    <thead>
                        <tr>
                            <th>Appointment ID </th> <th>Time </th> <th>Doctor</th> <th></th>
                        </tr>                
                    </thead>
                    <tbody id ='tableBody' class="card-body">

                    </tbody>

                </table>
            </div>
        </div>
            <div style="flex-grow: 1; height: 120px" class="card">
                <div class="card-body">
                    <ul>
                    <li><a href="/">Schedule Appointment (ADD LINK)</a></li>
                    <li><a href="/">Appointment History (ADD LINK)</a></li>
                    <li><a href="/">Billing (ADD LINK)</a></li>
                    </ul>
                </div>
            </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
</body>
</html>